# Decorator Text Editor (C++17, CMake)

## Build & Run

```bash
mkdir -p build
cd build
cmake ..
cmake --build .
./decorator_text_editor
```

Output is an HTML-like string showing how decorators wrap the base text.
